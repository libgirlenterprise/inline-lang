(uiop/package:define-package :inline.c/src/c (:nicknames :inline.c) (:use :cl)
                             (:shadow) (:export) (:intern))
(in-package :inline.c/src/c)
;;don't edit above

(defun c (filename contents)
  )
